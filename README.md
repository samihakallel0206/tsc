
#etapes
npm i -g typescript //install ts gloablement 
npm list -g         //?ts installé globalement
tsc --version       //version du ts
ou bien
npm i typescript // install dans le projet

tsc --init  // tsconfig.json (à configurer: option)


index.ts (du code)
tsc index (compiler en js)  ----> index.js (node index pour exe)
tsc -w //pour ne pas avoir a relancer la compilation a chque fois



